# Local development only. scripts/init_env.py generates a random password.
POSTGRES_PASSWORD=replace_with_a_random_password
APP_ORIGIN=http://localhost:8000
APP_ENV=development
SESSION_COOKIE_SECURE=false
